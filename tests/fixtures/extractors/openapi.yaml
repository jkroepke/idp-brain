---
openapi: 3.1.0
info:
  title: Fixture API
  version: 1.0.0
paths:
  /widgets:
    get:
      operationId: listWidgets
      summary: List widgets
      responses:
        "200":
          description: OK
components:
  schemas:
    Widget:
      type: object
      description: Widget response
      properties:
        name:
          type: string
